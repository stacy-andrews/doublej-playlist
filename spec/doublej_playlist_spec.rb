require 'spec_helper'

describe DoublejPlaylist do
  it 'has a version number' do
    expect(DoublejPlaylist::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
